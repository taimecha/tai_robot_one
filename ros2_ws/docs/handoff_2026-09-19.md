# Real Robot Handoff - 2026-09-19

## Update: 2026-09-21 charged-battery diagnostic plan

- The 24 V traction battery is now fully charged (100%).
- The previous rotation test was performed with a nearly discharged 24 V
  battery and is therefore inconclusive; do not use it to diagnose the
  recurring pose/alignment error.
- Keep the camera completely disabled. Continue with LiDAR-only localization
  and navigation using `use_camera:=false`.
- The LiDAR-to-map pose was recently adjusted against the full scan, but its
  correctness is not yet established. Revalidate localization and scan/map
  alignment after restart. Do not restore or modify the pose without explicit
  user confirmation.
- The next diagnostic is to restart the system, record LiDAR, raw wheel
  odometry/encoder data, IMU, EKF odometry, AMCL pose/particle state, TF and
  velocity commands, then perform one separately confirmed clockwise rotation
  of 10 degrees at approximately 0.35 rad/s.
- Compare encoder/wheel odometry, IMU yaw, EKF yaw and AMCL/LiDAR-map alignment
  before and after that rotation to isolate the source of the recurring error.
- Do not send a Nav2 goal, command motion, change pose or change configuration
  without explicit user confirmation.
- Only after reviewing the rotation data, and only after the user confirms
  physical clearance, test a forward motion of 1.0--1.3 m.
- Reported mechanical breakaway speeds are above 0.10 m/s linear and about
  0.30--0.35 rad/s angular.

## Results: 2026-09-21 charged-battery rotation diagnostics

- Camera remained disabled and all tests used LiDAR-only localization.
- A short clockwise command produced approximately 9.2--9.4 degrees in wheel
  odometry, integrated IMU gyro and EKF; AMCL reported approximately 9.0
  degrees. Angular scale agreement was good.
- Despite that angular agreement, AMCL introduced roughly 8--10 cm of map
  translation after the short turn and did not undo it during stationary
  no-motion updates. Do not proceed to the 1.0--1.3 m forward test yet.
- A first counter-clockwise retry did not move because the temporary ROS CLI
  publisher matched rosbag before `cmd_vel_stamper`; its non-zero messages did
  not reach `/base_controller/cmd_vel`. Subsequent CLI motion diagnostics must
  use `--wait-matching-subscriptions 2` while rosbag is recording.
- The valid counter-clockwise return produced only about 5.5 degrees wheel/EKF
  and 6.2 degrees IMU for a 0.5 second command, so it did not fully undo the
  preceding clockwise turn.
- Per-wheel ESP32 target and PWM telemetry is now exposed as the custom state
  interfaces `pid_target_velocity` and `pid_output_pwm` on
  `/dynamic_joint_states`. PID gains and firmware motion behavior were not
  changed.
- A commanded clockwise 45 degree diagnostic at 0.35 rad/s physically reached
  only about 32.4 degrees wheel/EKF and 32.8 degrees IMU. All four wheel
  targets reached 1.387 rad/s, but average measured/target ratios during the
  target plateau were FL 0.474, FR 0.600, RL 0.751 and RR 0.663. The front-left
  motor was slowest; the two rear motors responded faster.
- During that longer turn PWM remained modest (approximately 29--39, maximum
  40) and rose slowly. Production firmware currently uses one shared PID
  configuration for all motors (`Kp=0.3`, `Ki=0.02`, `Kd=0.01`, feed-forward
  gain 1.47 PWM/RPM and static-friction feed-forward 3 PWM). Do not change or
  tune these values without a separately confirmed plan.
- The unequal wheel response can move the instantaneous center of rotation and
  plausibly explains part of the apparent translation after an in-place turn.
  Fixed-wall scan alignment after the longer turn remained generally good,
  while some short/internal map features remained locally offset.

## Update: 2026-09-20 motion diagnostic

- User reports physical motion needs linear speed strictly above 0.10 m/s
  and angular speed at least 0.25 rad/s. These are reported breakaway limits,
  not permission to clamp collision-limited commands upward.
- LiDAR-only launch is implemented with `use_camera:=false`.
- A real goal from approximately (3.56, 0.10) to (3.47, 0.93) failed:
  first no progress, then rotation sweep blocked, followed by automatic spin
  and backup recovery. Goal was canceled. Earlier planner-only notes below
  are historical and no longer describe the latest motion-test status.
- Scan/map translation mismatch after motion remains unresolved. Do not reuse
  the old saved pose after this motion or assume extrinsic yaw is the cause.
- Next: record command stages, wheel odometry, EKF, IMU, AMCL, TF, scans,
  paths and costmaps before a separately confirmed short motion test.

## Known-good state

- Map: `maps/tai_map_real_20260918_112741.yaml`
- Localization validated on the real robot after mixed translation/rotation.
- AMCL real-hardware overrides in `launch/nav_real.launch.py`:
  - `update_min_d: 0.03`
  - `update_min_a: 0.02`
  - `max_beams: 120`
- Wheel separation multiplier: `1.62` in
  `config/ros2_controllers_real.yaml`.
- EKF fuses wheel odometry position/velocity and IMU angular velocity Z.
- Real LiDAR extrinsic yaw is currently calibrated to `+1.5 deg` in
  `description/robot_core.xacro`. The user reported this alignment as good.
- Camera geometry changes are saved (real camera pitch `-20 deg`, obstacle
  height filter `0.12..0.75 m`, depth `320x240@30`). Do not discard them.
- The Astra driver currently crashes with exit code `-11` when point-cloud
  generation is enabled in the full stack. A depth-only isolated test remained
  stable at about 29.6 Hz. Camera obstacle data must therefore be disabled for
  the next LiDAR-only Nav2 tests; debug the point-cloud crash later.
- Full Nav2 lifecycle reached active after fixing the collision-monitor
  `observation_sources` type and resetting/starting the navigation lifecycle.
- Planner-only tests succeeded. No real Nav2 motion goal has been sent yet.

## Last physical pose

The robot was not moved after the final planner-only tests. The last saved pose
was approximately:

```text
map x:   3.443 m
map y:   0.096 m
yaw:     109.6 deg
```

Leave the robot at the same physical position and heading if possible. Mark the
wheel/base position and fork direction before moving it. If it is moved while
powered off, set a new initial pose with RViz `2D Pose Estimate`; do not reuse
the numeric pose above blindly.

## Next session

1. Inspect `git status` and do not overwrite uncommitted changes.
2. Build `tai_robot_one`.
3. Start full real Nav2 with teleop disabled.
4. Open `config/nav_robot.rviz` on the laptop.
5. Restore the saved pose only if the robot is still on the marked spot.
6. First make a LiDAR-only Nav2 launch mode: do not start the Astra driver and
   remove/disable camera observation sources in the local costmap and collision
   monitor. Keep all camera-related source/config changes for later work.
7. Verify localization, LiDAR alignment, lifecycle state, costmaps, and zero
   velocity before sending any goal.
8. Ask the user to reconfirm the physically clear direction and complete fork
   sweep area before choosing the first real motion goal.

## Important safety state

- CM029 teleop and Nav2 must never publish velocity commands at the same time.
- Button B is a software stop, not a physical emergency stop.
- The front was reported blocked; the left side was reported clear.
- The final SE2 planner-only path succeeded, but physical clearance for the
  fork sweep still needs confirmation before movement.

## Update: 2026-09-21 charged-battery PID diagnostics

- The 24 V traction battery was reported fully charged. The robot was securely
  raised before the no-load wheel tests; these results must not be treated as
  loaded/floor performance.
- Firmware now uses per-wheel PID/feed-forward values: all wheels
  `Kp=2.0, Ki=0.02, Kd=0.01`; FL/FR feed-forward `2.40`, RL/RR `2.20`, with
  static-friction feed-forward `3`. This version was built and flashed.
- Added `/wheel_pid_telemetry` (`std_msgs/Float64MultiArray`) directly from the
  serial hardware plugin because `/dynamic_joint_states` was not reliably
  recorded. It publishes at 25 Hz with fixed order:
  `sequence, uptime_ms, [target, velocity, PWM, position] x FL/FR/RL/RR`.
- No-load bidirectional sweep at 0.35, 0.50, 0.65 and 0.80 rad/s is in
  `log/pid_noload_telemetry_035_080_20260921_153910`. Steady measured/target
  ratios were approximately: 0.35: FL/FR 111--115%, RL/RR 107--111%; 0.50:
  FL/FR 114--115%, RL/RR 109%; 0.65: FL/FR 116--117%, RL/RR 111--112%; 0.80:
  FL/FR 117--118%, RL/RR 112--113%. Both directions agreed closely.
- Plateau PWM rose only from about 29--30 at 0.35 rad/s to 58--60 at 0.80
  rad/s, with no saturation. The current no-load tune therefore overspeeds,
  especially on the front pair; do not infer the final loaded tune from this
  test alone.
- The hardware-only launch was restarted after building the telemetry change
  and was active at handoff. No floor translation test or Nav2 goal was run.
- A subsequent loaded right-turn test at a requested 0.35 rad/s physically
  turned right. Wheel odometry measured -16.6 degrees and EKF -17.0 degrees;
  loaded mean measured/target ratios were FL 79.6%, FR 83.0%, RL 91.3%, RR
  91.2%. The front pair remained slower than the rear pair. RViz was closed,
  so LiDAR-map/AMCL alignment was not observed during this test.
- User selected 0.25 rad/s as the minimum loaded rotation command. Nav2 Spin
  `min_rotational_vel` and the custom SafeRotationRPP active-turn floor were
  updated to 0.25 rad/s. Exact zero remains the stop command.
- LiDAR-only Nav2 was restarted with camera and CM029 teleop disabled. AMCL
  global localization did not converge while stationary, so the current scan
  was matched directly against the occupancy image. The unique best coarse
  match put about 84% of sampled scan endpoints within 0.13 m of mapped
  obstacles. The refined initial pose was `(3.175, 0.300, -110.5 deg)`; after
  six AMCL no-motion updates it converged to approximately
  `(3.166, 0.259, -111.6 deg)` with covariance x/y/yaw approximately
  `0.0186 / 0.0218 / 0.0107`.
- Navigation lifecycle initially remained partly inactive because planner
  activation had timed out before `map->odom` existed. Resetting and starting
  `lifecycle_manager_navigation` after localization succeeded; all Nav2
  servers were active and `/base_controller/cmd_vel_out` was exactly zero.
  No navigation goal was sent.
- After the user refined the pose visually, eight no-motion AMCL updates gave
  approximately `(3.164, 0.303, -112.4 deg)` with covariance
  `0.0557 / 0.0756 / 0.0254`. A confirmed-clear Nav2 Spin action then commanded
  a right 180-degree turn and succeeded in 13.5 s. Bag:
  `log/nav2_spin_right_180_20260921_164616`.
- Spin feedback reported -181.2 degrees, wheel odometry -184.6 degrees and EKF
  -186.5 degrees. AMCL reported -193.9 degrees and translated by 0.166 m
  (about `(3.167, 0.296)` to `(3.181, 0.130)`), reproducing the localization
  translation/yaw correction after an in-place turn.
- Loaded steady wheel measured/target ratios during this spin were FL 95.1%,
  FR 95.3%, RL 98.9%, RR 99.5%; PWM remained only about 26--35 (max 39).
  This is much more balanced than the earlier tune and points away from PID as
  the main remaining 16 cm AMCL shift.
- Although rosbag subscribed to `/imu`, it recorded zero IMU messages in this
  bag. Diagnose IMU QoS/publication separately before relying on another bag
  comparison. Do not run the planned 1 m translation until the post-spin scan
  alignment is visually checked.
- A user-requested, clear-area Nav2 Spin left 60 degrees subsequently
  succeeded in 5.3 s. Bag: `log/nav2_spin_left_60_20260921_164937`. Feedback
  reported +60.37 degrees, wheel odometry +63.91, EKF +65.60 and AMCL +66.36.
  Unlike the preceding 180-degree right turn, AMCL translated only 0.018 m.
- Loaded left-turn wheel ratios were FL 93.7%, FR 83.0%, RL 88.1%, RR 101.8%
  (PWM means about 30, 35, 31, 25). Direction-dependent imbalance remains,
  although localization stayed stable on this turn. `/imu` again recorded no
  samples because the publisher is BEST_EFFORT and the current rosbag-selected
  subscription did not receive it; use an explicit sensor-data QoS override.
- Visual inspection showed the scan still carried the earlier accumulated
  leftward offset. Direct post-spin scan matching selected approximately
  `(3.260, 0.120, 118.0 deg)`; after AMCL no-motion refinement the accepted
  pose was about `(3.237, 0.105, 118.4 deg)` with covariance below 0.009 in XY
  and 0.004 in yaw.
- The user confirmed the robot is not moved while powered off. Added
  `scripts/amcl_pose_persistence`, launched automatically by `nav_real`, to
  atomically store only low-covariance AMCL poses in
  `~/.ros/tai_robot_one/last_amcl_pose.json` and restore the last pose on the
  next startup. A live save/restore test reproduced the current pose without
  motion or manual RViz input.
- Real-hardware AMCL `alpha4` is overridden from 0.20 to 0.05 to reduce false
  translation injected by pure rotation. This change is built but takes effect
  only after the next full `nav_real` restart; PID was not changed.
- Full `nav_real` restart confirmed automatic pose restoration, active AMCL,
  map server and Nav2, camera disabled, and runtime `alpha4=0.05`. The user
  visually accepted the scan alignment after AMCL corrected a small manual
  pose offset during motion. The persisted pose is updated automatically.
- Post-restart right-10-degree validation bag:
  `log/alpha4_auto_adjust_right10_20260921_171320`. Nav2 feedback was -10.34
  degrees, but wheel odometry/EKF/AMCL measured about -14.84/-15.59/-14.00
  degrees. AMCL translated 0.028 m, largely undoing a preceding +3 cm manual
  pose adjustment; the user confirmed the final scan was aligned.
- A mistaken left-turn/partial-forward sequence was stopped. Bag:
  `log/left25_forward1m_20260921_171808`. Before stopping, wheel odometry
  measured roughly +30.0 degrees and 0.264 m translation. The user then asked
  to compensate to the originally intended right-25-degree heading.
- Compensation and straight-line validation are recorded in
  `log/compensate_back05_forward13_20260921_172126`: requested right 55
  degrees, back 0.5 m, then forward 1.3 m. All Nav2 behavior actions succeeded.
  Wheel odometry measured -58.51 degrees for the spin (EKF -60.12, AMCL
  -62.98, AMCL shift 0.019 m), 0.527 m backward with only +0.49 degree yaw,
  and 1.338 m forward with -1.00 degree yaw. AMCL measured 0.515 m backward
  and 1.306 m forward. Loaded straight wheel velocity/target ratios were tightly
  grouped around 103--106%; the right-spin ratios were FL/FR 80%, RL 88.6%,
  RR 91.3%. Translation performance is now substantially more balanced than
  rotation.
- Added an explicit `/imu` BEST_EFFORT/VOLATILE override to
  `config/rosbag_pid_qos.yaml`. Rosbag reports applying it and subscribing, but
  both `--all` and explicit-topic recordings still contain zero IMU samples,
  while the live graph shows the publisher and EKF/visualizer subscribers.
  Diagnose this recording path before claiming an encoder/IMU comparison.

## End-of-session state: 2026-09-21 17:58 ICT

- The user visually confirmed final LiDAR/map alignment at AMCL pose
  `(x=3.06048, y=0.00838, yaw=148.046 deg)`. Its covariance x/y/yaw was
  approximately `0.00475 / 0.00471 / 0.00334`. It is stored atomically in
  `/home/admin/.ros/tai_robot_one/last_amcl_pose.json`. Do not physically move
  the robot while powered off if this pose is to be restored tomorrow.
- Spin behavior was changed to 20 Hz, 0.25--0.30 rad/s, rotational acceleration
  0.6 rad/s^2, with velocity-smoother angular deceleration -1.5 rad/s^2.
- EKF now also fuses wheel-odometry yaw rate as a fallback. With the IMU absent,
  a right-25-degree retest improved Nav2 feedback to -25.01 degrees and reduced
  EKF-vs-wheel yaw disagreement to 0.34 degrees, but physical wheel odometry
  still reached -27.81 degrees. Bag:
  `log/spin_right25_ekf_wheel_yawrate_20260921_173552`.
- Large rotations still destabilize AMCL: a requested right 110 degrees gave
  wheel/EKF/AMCL approximately -113.41/-113.79/-116.26 degrees and 0.112 m AMCL
  translation. A later requested right 90 degrees gave about
  -94.57/-94.98/-100.92 degrees and 0.057 m AMCL translation. Bags:
  `log/right110_forward1_right90_20260921_174510` and
  `log/after_right110_forward1_right90_20260921_174757`.
- Full-scan offline matching was needed after AMCL became confidently wrong.
  Do not treat low AMCL covariance as proof of correct LiDAR/map alignment.
- The combined IMU/lift ESP32 was hard-reset successfully through RTS. Its
  uptime reset and lift telemetry continued, but `/imu` still produced no
  samples. Firmware retries `bno.begin()` every 2 seconds, so the likely fault
  is BNO055 power/wiring/I2C/address/hardware. The lift is `UNHOMED` after the
  reset. Inspect BNO055 3.3/5 V as appropriate, GND, SDA GPIO21, SCL GPIO22,
  and address 0x28/0x29 before further sensor calibration.
- One malformed manual `/initialpose` covariance caused a temporary NaN
  `map->odom`; the stack was restarted and recovered. Future scripted pose
  publications must construct exactly 36 covariance elements and use a zero
  timestamp so AMCL uses the latest odom transform.
- At shutdown handoff the base command output was exactly zero. Camera remains
  disabled and the active mode is LiDAR-only.
## Update: 2026-09-23 BNO055, EKF, and closed-loop turn validation

- The vehicle was physically moved while powered off. The old persisted AMCL
  pose was renamed to `~/.ros/tai_robot_one/last_amcl_pose.moved_robot_20260923.json`
  and must not be restored. AMCL still requires a new initial localization.
- Camera and Nav2 navigation remained disabled throughout these tests; only
  the LiDAR localization stack and real base were running.
- After repairing the BNO055 wiring, `/imu` held 50 Hz for a 416.8 s bag with
  no gaps over 0.1 s. Raw wheel odometry held 50 Hz and EKF `/odom` held 30 Hz.
- The old EKF fused wheel absolute yaw plus IMU gyro Z. During matched turns,
  its settled yaw returned to wheel yaw even though the BNO055 quaternion and
  integrated gyro consistently measured 3--5% more rotation.
- `config/ekf.yaml` now excludes wheel absolute yaw, retains wheel yaw-rate as
  a high-covariance fallback, and fuses BNO055 quaternion yaw plus gyro Z with
  `imu0_relative: true`. Encoder x/y pose and forward velocity remain fused.
- Post-change validation showed EKF versus IMU yaw RMS error `0.064 deg`, peak
  `0.39 deg`, and essentially zero final error. Encoder yaw remained about
  3--6% below BNO055 yaw on the tested loaded turns.
- `scripts/imu_visualizer` now shows relative yaw independently for IMU
  (pink), EKF (blue), and encoder (yellow). This is diagnostic display only;
  bags retain raw values. `config/localization_real.rviz` contains the three
  corresponding display layers.
- Added `scripts/turn_relative_yaw`, a watchdog-limited `/odom` feedback turn
  diagnostic. Valid speeds are 0.25--0.35 rad/s because lower commands do not
  reliably overcome loaded-platform friction. It repeatedly sends zero on
  completion or interruption.
- First right 90-degree closed-loop test (before the final braking adjustment)
  sent zero at `-89.204 deg`, coasted `-1.919 deg`, and stopped at
  `-91.123 deg`. The fixed braking allowance was increased from 0.8 to 1.9
  degrees. The following left return sent zero at `+88.289 deg`, coasted
  `+1.288 deg`, and stopped at `+89.576 deg` (0.424 deg short). Keep the common
  allowance until repeated trials justify direction-specific compensation.
- Resetting either ESP32 while ROS is running is not transparently recoverable:
  drive telemetry timed out, ros2_control deactivated, and the IMU bridge saw
  boot garbage. After resetting both ESP32 boards, restart the complete ROS
  stack before any motion test.

## Pause for Pi battery charging: 2026-09-23 13:xx ICT

- The robot remains physically stationary at the last test position. It is
  approximately yaw 180 degrees in the map (forks toward the left in the RViz
  view). Do not reuse this pose if the robot is moved while power is off.
- A right 90-degree localization trial showed IMU and EKF agreeing at about
  89.09 degrees, while the RViz robot appeared 3--4 degrees clockwise from the
  observed physical chassis and the scan shifted laterally. This exposed that
  the prior initial pose had been adjusted to the scan while hiding a likely
  LiDAR extrinsic error.
- Real LiDAR extrinsic yaw was changed experimentally from `+1.5 deg` to
  `-2.0 deg`. With the chassis set to a physical/map yaw of 180 degrees, the
  user reported that the scan then needed to move 5--10 cm backward along the
  vehicle (rightward on that RViz view).
- Real LiDAR extrinsic x was therefore changed experimentally from `-0.1365 m`
  to `-0.2115 m` (the midpoint, 7.5 cm farther rearward); y remains zero and z
  remains 0.385 m. These values are not yet validated by a second orientation
  and must not be treated as final calibration.
- The workspace built successfully after both changes. The stack was restarted
  with camera disabled and navigation disabled. The comparison pose was
  republished at approximately `(x=3.59, y=1.08, yaw=180 deg)`; AMCL reported
  `(3.59377, 1.07429)` immediately afterward. The user paused before reporting
  whether the new scan alignment was correct.
- On resume: start LiDAR-only localization, verify whether the saved pose is
  valid because the vehicle was not moved, inspect scan alignment without
  motion, then validate the extrinsic at a second heading 90 degrees away.
  Do not move until the user confirms clearance and recording is active.

## LiDAR extrinsic validation after charging: 2026-09-23 14:xx ICT

- The robot was not moved while powered off. The saved pose restored and AMCL
  settled near `(x=3.605, y=1.073, yaw=-179.31 deg)` with the experimental
  LiDAR extrinsic `x=-0.2115 m, y=0, yaw=-2.0 deg`. The user confirmed the
  scan matched the map while stationary.
- Validation bag:
  `log/lidar_extrinsic_x2115_yawminus2_validate_20260923_145237`.
- A closed-loop right 90-degree turn stopped at 89.25 degrees by EKF. IMU and
  EKF remained nearly coincident; raw wheel odometry again measured less yaw.
  Wheel odometry reported only about 1 mm false translation. AMCL settled near
  `(x=3.618, y=1.047, yaw=+91.4 deg)`, a map-position correction of about
  2.9 cm. The user confirmed that the scan still matched the map and that the
  vehicle display remained aligned at the second heading.
- Treat `x=-0.2115 m, yaw=-2.0 deg` as the current validated LiDAR extrinsic.
  Next validate translation while recording before enabling Nav2 navigation.

## EKF/AMCL correction and pose-persistence fix: 2026-09-23 15:xx ICT

- A forward test exposed an inconsistent EKF pose: raw wheel odometry retained
  an accumulated absolute heading while the EKF heading restarted near zero,
  but the EKF still fused the wheel's absolute map-axis x/y. AMCL interpreted
  the resulting apparent lateral motion as large rotations and dispersed.
- `config/ekf.yaml` now excludes wheel absolute x/y and yaw. It fuses wheel
  body-frame vx, the nonholonomic vy=0 constraint and wheel wz as fallback;
  BNO055 quaternion yaw and gyro z remain dominant. EKF x/y process-noise
  diagonals were reduced from 0.05 to 0.001.
- A recorded 0.20 m forward validation then ended at EKF x=0.2023 m,
  lateral=-0.00046 m and about -0.14 deg yaw change. AMCL moved consistently
  by about 0.20 m without global particle dispersion.
- The known-good post-test map pose is approximately
  `(x=3.615, y=0.393, yaw=-87.96 deg)`. The user visually confirmed alignment
  after this pose was republished. The current persistence JSON contains this
  pose; do not substitute the earlier y=0.545 startup estimate.
- Root cause of that stale startup estimate was found in
  `scripts/amcl_pose_persistence`: its `/amcl_pose` callback could overwrite
  the state file during the two-second interval before restore. Saving is now
  disabled until the restore attempt completes and for another five seconds
  while AMCL consumes `/initialpose` and converges. The package builds and the
  script passes Python syntax compilation; a full restart validation remains
  to be performed without moving the robot.
- Camera and Nav2 navigation remain disabled. The robot is stationary.
