# Keep every TAI Robot One process on the same ROS middleware.  An explicit
# value set before sourcing the workspace remains authoritative.
if [ -z "${RMW_IMPLEMENTATION:-}" ]; then
  export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
fi
